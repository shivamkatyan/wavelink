# Rust toolchain pin. Channel "stable" tracks the current stable release.
# Observed current stable at pin time (2026-09-06): 1.98.1 (48a229cea 2026-09-01)
# Dist channel date: 2026-09-03.
[toolchain]
channel = "stable"
components = ["rustfmt", "clippy"]
profile = "minimal"
