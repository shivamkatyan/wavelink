# t-B0-devex — DevEx Bootstrap & Skeleton Report

Task: `t-B0-devex` · Root: `R-B0-DEVEX` · Owner: DevEx · Status: **complete** · Date: 2026-09-06

## What was created (all within allowed path set)

| Path | Purpose |
|------|---------|
| `rust-toolchain.toml` | Pins `channel = "stable"` + `rustfmt`/`clippy` components, `profile = "minimal"`. Observed current stable from dist server: **1.98.1 (48a229cea 2026-09-01)**; channel date 2026-09-03. |
| `dev/bootstrap` | Idempotent, noninteractive. Installs rustup (`-y --profile minimal`, bounded retries), pinned stable toolchain, `just` (cargo install → apt fallback); warns (does not fail) if docker missing; writes `dev/env.sh`; prints env summary. `set -euo pipefail`. |
| `dev/env.sh` | Sourceable PATH/CARGO_HOME/RUSTUP_HOME exports for the current shell. |
| `justfile` | Recipes: `format`, `lint`, `unit`, `build`, `selftest` (unit+lint), `bench`/`package`/`clean-verify` stubs. |
| `Cargo.toml` | `[workspace]` members `crates/*`, `resolver="2"`, `workspace.package` metadata, edition 2021, rust-version 1.98. |
| `crates/wdr_dev/` | Placeholder crate: `host_banner() -> "wdr-dev-ready"` and `dev_env_name()` (reads `WDR_DEV_ENV` with fallback) + 2 unit tests. |
| `.github/workflows/ci.yml` | 5 jobs, **all gated `if: false`**: `linux-core`, `windows-basic` (WASAPI TODO), `macos-basic` (taps TODO), `android-emulator` (emulator-runner composite), `ios-simulator`. |
| `.dockerignore` | Excludes target/, git, dev/, docs/, `.env`, keystores. |
| `Dockerfile.dev` | Ubuntu 24.04 + rustup stable minimal + cmake, pkg-config, libasound2-dev, libdbus-1-dev, libssl-dev, iproute2/netcat. No protobuf (postcard planned in B0 workspace). |
| `compose.yml` | `emitter-sim`, `receiver-sim` (plain), `netem` (**NET_ADMIN only**) — all `sleep infinity` placeholders; wiring in B1. |
| `NOTES_B0_DEVEX.md` | This report. |

## Exact commands to run

```bash
./dev/bootstrap                # idempotent; run once (or re-run freely)
source dev/env.sh              # add tools to PATH for this shell
just format                    # cargo fmt --all -- --check
just lint                      # cargo clippy --all-targets --all-features -- -D warnings
just unit                      # cargo test -p wdr_dev
just build                     # cargo build --workspace
just selftest                  # unit + lint
docker compose build && docker compose up -d   # harness (B1 wiring pending)
```

## Verification performed

- `bash -n dev/bootstrap` → syntax OK (also after the retry edit).
- `./dev/bootstrap` ran 3× — all idempotent; installs ran once, later runs skipped.
- `curl https://static.rust-lang.org/dist/channel-rust-stable.toml` confirmed stable `1.98.1`.
- `just --dry-run format` → parses; `just --list` lists all 8 recipes.
- `cargo build --workspace` → OK.
- `cargo test -p wdr_dev` → 2 passed.
- `cargo clippy --all-targets --all-features -- -D warnings` → clean (`wdr_dev` only).
- `docker compose config --quiet` → valid.
- Both workflow YAMLs parse as valid YAML.

## Host gaps / notes

- Rust was **not** installed at start; `./dev/bootstrap` installs rustup + stable 1.98.1 + just 1.58.0 (~2 min for `cargo install just`).
- First `sh.rustup.rs` fetch hit a transient curl failure; bootstrap now retries (implemented after observing; did not fake success — the first run errored, second succeeded).
- Docker 29.7.2 present; container build not exercised (B1).
- Windows/macOS/Android/iOS CI jobs are stubs gated `if:false`; hosted runners lack audio endpoints (see DEVELOPMENT_ENVIRONMENT.md).
- A pre-existing `.github/workflows/license-audit.yml` exists (ossdep worker's scope, also gated `if:false`); not touched. `Cargo.lock` was generated by the first build and is left untracked (non-ignored); the dependency policy commits it — left to supervisor/ossdep integration.
- Nothing committed (supervisor handles integration).

## Out of scope

No product features; no edits to docs/ or other crates; no git commits made.
